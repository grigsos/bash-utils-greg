# AWS
alias whoamiaws='aws sts get-caller-identity'

# VSCode
alias c='code .'

# Misc
alias ll="ls -al"
alias clr="clear"
alias python="python3"
alias rr="rm -rf"

# Terraform
alias t="terraform"
alias tia="terraform init && terraform apply -parallelism=15"
alias tiay="terraform init && terraform apply -auto-approve -parallelism=15"

# Kubernetes
alias k="kubectl"

# Python virtualenv
alias venvup='source venv/bin/activate'

# Git
alias g="git"
alias gpull="git pull"
alias gpush="git push"
alias gs="git status"
alias gc="git commit -m"
